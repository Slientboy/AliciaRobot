from AliciaRobot import (
    DEV_USERS, 
    DRAGONS, 
    DEMONS, 
    TIGERS, 
    WOLVES, 
    telethn
)

HIGHER_AUTH = DRAGONS + WOLVES + DEMONS + TIGERS + DEV_USERS

HIGHER_AUTH = list(DRAGONS) + list(WOLVES) + list(DEMONS) + list(TIGERS) + list(DEV_USERS)

HIGHER_AUTH.append(1087968824)
